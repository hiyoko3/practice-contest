import sys
# sys.exit() 強制終了

# 辞書のソート
# key
# s = sorted(s.items(), key=lambda x:x[0])
# val
# s = sorted(s.items(), key=lambda x:x[1])

def hello():
  print("Hello World")

# hello()

# 配列もこれでとれる
N,M = map(int,input().split())
