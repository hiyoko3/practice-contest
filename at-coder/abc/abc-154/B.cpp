#include <bits/stdc++.h>
#define repeat(i, n) for(ll (i) = 0; (i) < (n); (i)++)
using namespace std;
typedef long long ll;

int main() {
	string S;
	cin >> S;

	repeat(i, S.length()) {
		cout << "x";
	}
	cout << endl;
	return 0;
}
